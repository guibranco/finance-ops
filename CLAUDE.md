## Agent skills

### Issue tracker

Issues are tracked in GitHub (guibranco/finance-ops), using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Mapped to the repo's existing vocabulary (`🚦 awaiting triage`, `⏳ waiting response`, `wontfix`) plus `ready-for-agent` / `ready-for-human`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
